Grid occupies the whole height of the web page.
Rows in the grid don't occupy the the whole height of the web page.
When addThemeVariants(GridVariant.LUMO_COLUMN_BORDERS) is applied to the grid, column border is broken,
i.e. there is no divider line displayed under the bottom row.
